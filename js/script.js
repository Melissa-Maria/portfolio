/* document.body.addEventListener('mouseover', e => {
    let hover = e.target.closest('.hover');
    if (!hover) { return; }
    hover.style.backgroundColor = '#B7ADCF';
  });
  
  document.body.addEventListener('mouseout', e => {
    let hover = e.target.closest('.hover');
    if (!hover) { return; }
    hover.style.backgroundColor = '';
  }); */

/* var image = document.getElementByClass('hover');

image.addEventListener('mouseover', function() { 
    image.setAttribute('style','-webkit-filter: brightness(2)'); 
}, false);

image.addEventListener('mouseout', function() { 
    image.setAttribute('style','-webkit-filter: brightness(1.0)'); 
}, false); */
